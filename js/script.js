document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('a[href^="#"]').forEach(anchor=>{anchor.addEventListener('click',function(e){e.preventDefault();const targetId=this.getAttribute('href');if(targetId==='#home'){window.scrollTo({top:0,behavior:'smooth'})}else{document.querySelector(targetId).scrollIntoView({behavior:'smooth'})}})});const backToTopBtn=document.getElementById('backToTopBtn');window.addEventListener('scroll',()=>{if(window.scrollY>300){backToTopBtn.style.display='flex'}else{backToTopBtn.style.display='none'}});backToTopBtn.addEventListener('click',e=>{e.preventDefault();window.scrollTo({top:0,behavior:'smooth'})});const productDetailModal=document.getElementById('productDetailModal');if(productDetailModal){productDetailModal.addEventListener('show.bs.modal',event=>{const button=event.relatedTarget;const productName=button.getAttribute('data-product-name');const productDescription=button.getAttribute('data-product-description');const productPrice=button.getAttribute('data-product-price');const productUnit=button.getAttribute('data-product-unit');const modalTitle=productDetailModal.querySelector('#productDetailModalLabel');const modalProductName=productDetailModal.querySelector('#modalProductName');const modalProductDescription=productDetailModal.querySelector('#modalProductDescription');const modalProductPrice=productDetailModal.querySelector('#modalProductPrice');const modalProductUnits=productDetailModal.querySelector('#modalProductUnits');modalTitle.textContent=`Detail ${productName}`;modalProductName.textContent=productName;modalProductDescription.textContent=productDescription;modalProductPrice.textContent=`Rp ${productPrice} / ${productUnit}`;modalProductUnits.innerHTML=''})}})